import os


def test(save_folder, config, model_name, data_names):
    
    
    if not os.path.exists(save_folder):
        os.makedirs(save_folder)
        
    for name_num,name in enumerate(names):
        
        img=imread(name)
        
        
        
        
        
        
    